{
  "version": 2,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "vercel_output",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}